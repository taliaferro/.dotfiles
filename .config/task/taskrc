# Files
data.location=~/.local/share/task
hooks.location=~/.config/task/hooks

# Color theme (uncomment one to use)
include dark-16.theme
news.version=2.6.0

# Reports

report.urgent.columns = id,priority,project,due,description,urgency
report.urgent.labels  = ID,P,Project,Due,Description,Urg
report.urgent.filter  = status:pending limit:10
report.urgent.sort    = urgency-

# User-Defined Attributes

uda.issue.type = string
uda.issue.label = Issue
